import { Components } from '@mui/material';

export const components: Components = {
  MuiTextField: {
    defaultProps: {
      margin: 'dense',
      fullWidth: true,
      size: 'small',
      variant: 'outlined',
    },
  },
  MuiFormControl: {
    defaultProps: {
      margin: 'dense',
      fullWidth: true,
      size: 'small',
    },
  },
};
